"""Person 1 / Person 3 persistence boundary.

The foundation intentionally runs in memory. Add the SQLAlchemy engine and
repositories here, then inject repositories into services instead of querying
the database from routers or agents.
"""

